# Type Path                            Mode  User  Group Age Argument

### BOINC ###
# Ensure the directory exists
d      /var/lib/boinc                  0755  boinc boinc -   -
# Ensure the auth file exists and has restricted perms
f+     /var/lib/boinc/gui_rpc_auth.cfg 0600  boinc boinc -   -
# Recursively fix owner or context only
Z      /var/lib/boinc                  -     boinc boinc -   -
